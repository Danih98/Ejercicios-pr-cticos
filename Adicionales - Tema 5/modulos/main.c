/* Seccion de declaraciones */
#include "modulo.h"

/* Seccion de funcionalidad del programa */
int main() {
    int valor;
    // parte inicial del programa (se omite el codigo)
    valor = f1();
    // mas instrucciones de codigo
    f2();
    return 0;
}