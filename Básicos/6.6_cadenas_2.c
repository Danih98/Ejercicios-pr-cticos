#include <stdio.h>
#include <string.h>

int main() {
    char cadena[] = "Holaaa. Mi nombre es Ioritz"; // Declara una cadena de caracteres

    int longitud = strlen(cadena); // Calcula la longitud de la cadena

    printf("Longitud de la cadena: %d\n", longitud); // Muestra la longitud

    return 0;
}

